import PageStub from "../components/PageStub";

export default function Rips() {
  return (
    <PageStub
      title="RIPs"
      subtitle="Retail Incentive Programs — yield ranker, tier break-even, stability badge."
    />
  );
}
