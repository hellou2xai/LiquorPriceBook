import PageStub from "../components/PageStub";

export default function Catalog() {
  return (
    <PageStub
      title="Catalog"
      subtitle="Browse and filter every SKU in the current price book."
    />
  );
}
